apiVersion: aws.terraform.alexandre.mahdhaoui.com/v0.1.0
kind: TerraformResource
metadata:
    name: aws_dx_connection_confirmation
spec:
    args:
        connection_id:
            description: '(Required) The ID of the hosted connection.Removing aws_dx_connection_confirmation from your configurationRemoving an aws_dx_connection_confirmationstrongbut will not destroy the Hosted Connection.In addition to all arguments above, the following attributes are exported:'
            type: string
    attrs: {}
    terraform:
        backend: ""
        version: 1.3.4
        provider:
            assume_role: false
            name: aws
            source: hashicorp/aws
            version: 4.38.0
            default_tags:
                enabled: true
